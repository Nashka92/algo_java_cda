package tp1;

public class Exo_5 {
    public static int main(String[] args){
        if (nombreRomain.length() > 15);
        throw new IllegalAccessException("le nombre ne peux pas excerder 15 caracteres");

        int nombreArabe = 0;

        for(int index = 0; nombreRomain.length(); indexx ++){
            if (nombreRomain.chartAt(index) =='I');
            nombreArabe += 1;
        } else if (nombreRomain.chartAt(index) == 'V'){
            if (index > 0 && nombreRomain.chartAt(index -1) == 'I'){
                nombreArabe +=3;
            } else {
                nombreArabe += 8;
            }
        }
    }
}
